using MakeMapObjectToLibForCut;
using System;

namespace MapObjectToLibTilesForCut // Note: actual namespace depends on the project name.
{
    class Program
    {
        static void Main(string[] args)
        {
            MainClass mainClass = new MainClass();
            mainClass.Main(args);
        }
    }
}